package ru.turlyunef.focusstart.turlyun.common;

public enum MessageType {
    DATA,
    REQUEST_NAME,
    USER_NAME_RESPONSE,
    REQUEST_USERS_NAME,
    USERS_NAME_RESPONSE,
    WRONG_NAME,
    CONNECTED,
    DISCONNECTED,
    NEW_USER_CONNECTED,
    USER_DISCONNECTED
}